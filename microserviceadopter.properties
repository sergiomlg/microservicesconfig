server.port=9092

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

#datasource config
spring.datasource.url=jdbc:mysql://${DB_IP:localhost}:${DB_PORT:3306}/${DB_NAME:microserviceadopter}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Pa55w.rd1!}

#eureka config
eureka.client.service-url.defaultZone=http://${EUREKA_IP:localhost}:${EUREKA_PORT:9999}/eureka

eureka.instance.prefer-ip-address=true